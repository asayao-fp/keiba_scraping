[project]
name = "keiba-scraping"
version = "0.1.0"
description = "Keiba prediction MVP (Windows venv)."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]